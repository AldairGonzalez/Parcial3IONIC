import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Parcial3IONIC',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
